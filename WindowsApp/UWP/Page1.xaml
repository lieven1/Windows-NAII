<Page
    x:Class="UWP.Page1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:UWP.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView x:Name="lijst" ItemsSource="{x:Bind reizen}" HorizontalAlignment="Left" VerticalAlignment="Top">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Reis">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Naam}"></TextBlock>
                        <TextBlock Text="{x:Bind Vertrek}"></TextBlock>
                        <TextBlock Text="{x:Bind Terug}"></TextBlock>
                        <Line Stroke="Red" X1="0" X2="300" Y1="5" Y2="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Content="Add player" Click="Button_Click"/>
    </Grid>
</Page>
